name: Validate Licenses

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'package.json'
      - 'pnpm-lock.yaml'
  push:
    branches:
      - develop
    paths:
      - 'package.json'
      - 'pnpm-lock.yaml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-licenses:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Validate dependency licenses
        shell: bash
        run: |
          set -o pipefail
          pnpm run validate-licenses 2>&1 | tee license-validation.txt
      - name: Upload license validation output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: license-validation-output
          path: license-validation.txt
          if-no-files-found: ignore
