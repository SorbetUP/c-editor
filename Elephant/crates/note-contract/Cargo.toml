[package]
name = "elephant-note-contract"
version = "0.1.0"
edition = "2021"
rust-version = "1.91"
license = "MIT"
description = "Vendored public-build contract for the private Elephant Note domain crate"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
