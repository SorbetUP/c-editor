[package]
name = "elephant-draw"
version = "0.1.0"
edition = "2021"
rust-version = "1.91"
license = "MIT"
description = "Rust-native Excalidraw-compatible document and interaction core"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
