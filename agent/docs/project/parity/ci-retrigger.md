# CI retrigger note

This file exists to retrigger the PR checks after CI/build configuration changes on assistant branches.

Latest retrigger reason: validate Electron/Tauri builds after native runtime imports were moved out of the Electron bundle.

It can be removed after the CI workflow is confirmed stable on assistant branches.
