[package]
name = "elephant-enaddon-packager"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
blake3 = "1.8"
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
