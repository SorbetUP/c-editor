# Legacy

Ce dossier conserve les sources historiques qui ne font plus partie du chemin de build principal.

- `macos-root/` : anciens fichiers Objective-C precedemment stockes a la racine

Avant de reintroduire un fichier depuis `legacy/`, verifier qu'il est encore reference par le build, les tests ou la documentation.
